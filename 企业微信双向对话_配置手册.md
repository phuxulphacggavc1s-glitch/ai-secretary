# 企业微信双向对话 · 配置手册（约 20 分钟）

> 做完这个，你就能在企业微信里直接跟秘书"小e"聊天：记待办、问建议、回进展，
> 它的回复会像同事发消息一样弹到你手机上。
> 代码已就绪，你只需要在企业微信管理后台操作 + 填 6 个环境变量。

## 你要拿到的 6 个值

| 环境变量 | 是什么 | 在哪拿 |
|---|---|---|
| WECOM_CORP_ID | 企业ID | 管理后台 → 我的企业 → 企业信息 最下方 |
| WECOM_APP_SECRET | 应用密钥 | 应用详情页 → Secret → 查看 |
| WECOM_APP_AGENT_ID | 应用ID | 应用详情页 → AgentId |
| WECOM_APP_TOKEN | 回调校验令牌 | 设置接收消息时点"随机获取" |
| WECOM_APP_AES_KEY | 回调加密密钥 | 同上，点"随机获取" |
| WECOM_APP_USER_MAP | 账号映射 | 见第 5 步 |

## 操作步骤（顺序很重要，别跳）

**第 1 步：创建自建应用**
电脑打开 https://work.weixin.qq.com 管理后台 → 应用管理 → 自建 → 创建应用。
名字填「AI秘书小e」，可见范围选你自己。创建后记下 **AgentId** 和 **Secret**。

**第 2 步：拿企业ID**
我的企业 → 企业信息 → 页面最下方复制**企业ID**。

**第 3 步：生成回调的 Token 和 AESKey（先别点保存！）**
应用详情 → 接收消息 → 设置API接收：
- URL 填：`https://mede-in-ai.com/api/wecom/callback`
- Token 和 EncodingAESKey 都点"随机获取"，**把两个值复制出来**
- ⚠️ 这一页先开着不要点保存——点保存时企业微信会立刻验证 URL，
  必须等服务器配好才能验证通过（第 6 步再回来点）。

**第 4 步：配企业可信IP**
应用详情往下翻 → 企业可信IP → 填你 ECS 服务器的公网 IP。
（不填的话秘书发不出回复，报错 60020。）

**第 5 步：拿两个账号 ID，拼出映射**
- 企业微信 userid：通讯录 → 点你自己 → 「账号」那一栏（比如 `YiXu`）
- 应用用户 UUID：Supabase 后台 → Authentication → Users → 复制你邮箱账号的 UUID
- 拼成：`{"YiXu": "你的uuid"}`

**第 6 步：服务器填配置并重启**（阿里云 Workbench 粘贴，注意替换成真实值）

```bash
cat >> /var/www/ai-secretary/backend/.env << 'EOF'
WECOM_CORP_ID=ww你的企业ID
WECOM_APP_SECRET=你的应用Secret
WECOM_APP_AGENT_ID=1000002
WECOM_APP_TOKEN=第3步的Token
WECOM_APP_AES_KEY=第3步的AESKey
WECOM_APP_USER_MAP={"你的userid": "你的supabase-uuid"}
EOF
cd /var/www/ai-secretary/backend && source venv/bin/activate && pip install -r requirements.txt
systemctl restart ai-secretary
```

**第 7 步：回到第 3 步那一页，点保存**
显示"保存成功"就是通了。如果报"回调验证失败"，检查：
① 服务器重启了吗 ② Token/AESKey 复制时有没有多空格 ③ URL 是不是 https 且拼写正确。

**第 8 步：测试**
手机企业微信 → 工作台 → AI秘书小e → 发一句「在吗」。
它回你，就成了。再试试「明天下午3点提醒我给供应商打款」和「今天先做什么」。

## 它在企业微信里能干什么

- 说一句话记待办（自动识别时间、分类、优先级，直接建好，不用确认弹窗）
- 问建议、汇报进展（和网页版同一个大脑、同一份记忆，两边打通）
- 聊天中提到的重要事实（账期、客户习惯）会自动记进长期记忆
- 原有的督办提醒、早报照旧推送——现在你可以直接回它了

## 安全说明

回调接口不需要登录，但有三道锁：企业微信签名校验（SHA1）、消息体 AES 解密
（密钥只有你和企业微信有）、CorpID 校验。别人伪造不了请求。
