# 让 AI 秘书"手机随时随地能用" — 部署清单（海外免费方案）

> 目标：把项目部署到云端，拿到一个网址，手机在任何网络下打开就能用。
> 方案：前端用 **Vercel**、后端用 **Railway**、数据库继续用 **Supabase**。全部有免费额度，个人自用基本零成本。
> 本清单分两块：**【Codex 自动做】**（改文件、提交、推 GitHub）和 **【你手动做】**（注册账号、网页点按钮、填密钥）。

---

## 第 0 步：先搞清楚现在到哪了（我已帮你确认）

- ✅ 部署配置文件都已就绪：`backend/Procfile`、`backend/railway.toml`、`frontend/vercel.json`
- ✅ 代码已连接 GitHub 远程仓库：`ai-secretary`
- ⚠️ 本地有一批**还没提交**的改动（上次的安全修复：限流、长度校验、邮件转义等）。**部署前必须先提交并推上去**，否则线上跑的是旧代码。
- ⚠️ 还没在 Railway / Vercel 上做实际部署。

所以接下来就两件事：**先让 Codex 把代码推干净，再由你点几下完成上线。**

---

## 第一块：【Codex 自动做】把代码准备好并推到 GitHub

> 下面整段可以直接复制给 Codex。它做完后你再做第二块。

```
背景：项目要部署到 Vercel(前端) + Railway(后端)。当前本地有未提交的安全修复改动，
需要先验证、提交、推送到 GitHub。仓库已配置 origin。

【任务 1：确认没有密钥会被提交】
- 运行 git status，列出所有改动
- 确认 backend/.env、frontend/.env.local、DEPLOY_ENV.txt 都在 .gitignore 中、
  且不在待提交列表里
- 如果发现任何 .env 文件出现在待提交列表，立即停止并告知我
验收：git status 里看不到任何 .env / DEPLOY_ENV.txt。

【任务 2：本地快速自检后端能启动】
- cd backend
- 确认 requirements.txt 里包含限流用的依赖（如新增了 slowapi 要在内）
- 运行已有测试：python -m pytest -q（若有失败，先修复或告诉我原因，不要硬提交）
验收：测试通过，或明确列出失败项。

【任务 3：确认前端能正常打包】
- cd frontend
- 运行 npm install 和 npm run build
- 确认 dist 目录能成功生成、无报错
验收：npm run build 成功。

【任务 4：提交并推送】
- 回到项目根目录
- git add 仅添加源码与配置改动（不要 add 任何 .env）
- git commit -m "chore: 安全修复 + 部署就绪"
- git push origin main
- 推送后运行：git show HEAD --name-only | grep -E "\.env" && echo "危险" || echo "安全：无env文件"
验收：推送成功，且确认没有 env 文件被推上去。

【任务 5（可选，防部署踩坑）：固定 Python 版本】
- 在 backend/ 下创建 runtime.txt，内容一行：python-3.12.3
  （Railway 默认可能用别的版本，固定一下更稳妥；若你确认本地用的是 3.11/3.13，
   就把版本号改成对应的）
- 一并 commit & push
验收：backend/runtime.txt 存在并已推送。
```

---

## 第二块：【你手动做】在网页上完成上线（非技术，照着点）

> 这一块 Codex 帮不了，因为要登录你自己的账号、在网页上操作。每步都很简单。
> 准备好：你的 GitHub 账号、本地 `backend/.env` 和 `frontend/.env.local` 文件里的密钥值（复制用）。

### 步骤 A：部署后端到 Railway

1. 打开 https://railway.app ，用 GitHub 账号登录。
2. 点 **New Project** → **Deploy from GitHub repo** → 选你的 `ai-secretary` 仓库。
3. 进入项目后，找到 **Settings → Root Directory**，填 `backend`（告诉它后端在这个子目录）。
4. 找到 **Variables（环境变量）**，把下面这些**逐条**加进去。值从你本地 `backend/.env` 文件里复制：
   ```
   SUPABASE_URL=（从 .env 复制）
   SUPABASE_SERVICE_KEY=（从 .env 复制）
   DEEPSEEK_API_KEY=（从 .env 复制）
   DEEPSEEK_BASE_URL=https://api.deepseek.com
   RESEND_API_KEY=（从 .env 复制）
   FROM_EMAIL=（从 .env 复制，如 onboarding@resend.dev）
   SECRET_KEY=（从 .env 复制，随便一串也行）
   FRONTEND_URL=（先留空，等步骤 B 拿到前端网址后回填）
   ```
5. 等它自动部署完成（几分钟）。成功后 Railway 会给你一个后端网址，形如
   `https://xxx.up.railway.app`，**复制记下来**。
6. 验证：在浏览器打开 `你的后端网址/health`，看到 `{"status":"ok"}` 就说明后端活了。

### 步骤 B：部署前端到 Vercel

1. 打开 https://vercel.com ，用 GitHub 账号登录。
2. 点 **Add New → Project** → 选 `ai-secretary` 仓库。
3. **Root Directory** 填 `frontend`。
4. **Environment Variables（环境变量）** 里加：
   ```
   VITE_SUPABASE_URL=（从 frontend/.env.local 复制）
   VITE_SUPABASE_ANON_KEY=（从 frontend/.env.local 复制）
   VITE_API_BASE_URL=（填步骤 A 拿到的后端网址，如 https://xxx.up.railway.app）
   ```
5. 点 **Deploy**，等几分钟。成功后 Vercel 给你一个前端网址，形如
   `https://xxx.vercel.app`，**这就是你手机要打开的网址**。

### 步骤 C：把前端网址回填给后端（很重要，否则会报跨域错误）

1. 回到 Railway → Variables。
2. 把 `FRONTEND_URL` 改成步骤 B 拿到的 Vercel 网址（如 `https://xxx.vercel.app`）。
3. Railway 会自动重启后端。等一两分钟。

### 步骤 D：手机上用起来

1. 手机浏览器打开你的 Vercel 网址，注册/登录，试着输入一句"明天下午提醒我给客户报价"。
2. 想要"像 App 一样"：
   - **iPhone（Safari）**：点底部分享按钮 → "添加到主屏幕"。桌面就会多一个图标，点开和 App 一样。
   - **安卓（Chrome）**：右上角菜单 → "添加到主屏幕"。
3. 以后无论手机用 5G 还是别的 WiFi，点这个图标就能用，跟电脑完全无关了。

---

## 常见问题排查（出错了对照看）

- **手机打开页面能显示，但登录/保存任务报错** → 八成是步骤 C 没做，或后端环境变量填错。先打开 `后端网址/health` 看后端是否正常，再检查 Railway 的 `FRONTEND_URL` 是否等于 Vercel 网址。
- **Railway 部署失败、日志报 Python 版本或依赖错误** → 做一下第一块的"任务 5"固定 Python 版本，再重新部署。
- **提示跨域 / CORS 错误** → 确认 `FRONTEND_URL`（Railway）和实际访问的 Vercel 网址**完全一致**（含 https，结尾别多斜杠）。
- **国内打开偶尔很慢或打不开** → 这是海外免费方案的固有缺点（Vercel/Railway 在海外）。自己用问题不大；如果以后要给客户/团队用且追求稳定，再考虑换国内云服务器。
- **收不到邮件提醒** → 检查 Railway 里 `RESEND_API_KEY` 和 `FROM_EMAIL` 是否填了；Resend 免费版默认发件域名是 `onboarding@resend.dev`。

---

## 一句话总结

让 Codex 跑完**第一块**（把安全修复提交推上去），然后你照着**第二块**在 Railway 和 Vercel 点十来下，拿到一个 `xxx.vercel.app` 网址加到手机桌面——就完成了，以后手机随时随地能用，和电脑、和网络都没关系了。
