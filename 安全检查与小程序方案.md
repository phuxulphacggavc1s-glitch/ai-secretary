# AI 秘书 — 安全检查报告 + 微信小程序可行性 + Codex 执行方案

> 检查对象：AI_Secretary（FastAPI 后端 + React 前端 + Supabase 数据库 + DeepSeek + Resend 邮件）
> 检查日期：2026-06-03
> 本报告只做评估，不改动任何代码。文末给出可直接交给 Codex 执行的任务清单。

---

## 第一部分：安全检查报告

### 总体结论

**当前安全水平：及格偏上，可以小范围上线，但有 2 个"中风险"必须在放给更多人用之前处理。**

先说让我放心的几点（你已经做对了）：

- **密钥没有泄露到 GitHub。** 我翻了 git 全部历史，`.env`、`.env.local`、`DEPLOY_ENV.txt` 都被 `.gitignore` 挡住了，提交记录里只有占位符（`your-xxx-key`），没有真实密钥。这是最容易翻车的地方，你躲过了。
- **每个接口都做了"只能看自己数据"的过滤。** 增删改查 4 个接口，每一条都带了 `user_id = 当前登录用户`，别人拿不到你的数据。
- **数据库开了行级安全（RLS）。** `tasks`、`daily_reports`、`users` 三张表都开了，并且策略是"只能访问 `auth.uid()` 等于自己的行"。
- **登录用的是 Supabase 官方校验**，每个请求都拿 token 去 Supabase 验真伪，没有自己手写一套容易出错的鉴权。

下面是风险清单，按严重程度排。每条我都用大白话说清楚"是什么、为什么危险、怎么修"。

---

### 🔴 高风险：暂无

目前没有"会被人直接盗号、删库、批量拖走全部用户数据"这种级别的洞。这一点值得肯定。

---

### 🟡 中风险（上线前建议处理）

**M1. AI 解析接口没有限流，可能被人刷爆你的 DeepSeek 账单**

- **是什么**：`/tasks/parse` 这个接口，每调用一次就会请求一次 DeepSeek（要花钱）。现在只要是注册用户就能无限次调用，没有任何"每分钟最多几次"的限制。
- **为什么危险**：如果有人注册一个账号，写个脚本一秒钟请求几十次，你的 DeepSeek 余额会被快速烧光。对个人小项目，这是最现实的损失——不是数据被偷，而是钱包被偷。
- **怎么判断严不严重**：取决于你 Supabase 的注册设置。如果"邮箱注册不需要验证就能用"，那任何人都能注册→任何人都能刷。先去 Supabase 后台确认这一点。
- **怎么修**：① 给 `/tasks/parse` 加限流（比如每个用户每分钟最多 10 次）；② 在 DeepSeek 后台设置每月消费上限（最省事的兜底）；③ Supabase 开启"邮箱必须验证才能登录"。

**M2. 后端用的是 Supabase"超级管理员钥匙"，安全全靠代码不出错**

- **是什么**：后端连数据库用的是 `SUPABASE_SERVICE_KEY`（服务角色密钥）。这把钥匙的特点是——**它会绕过你辛辛苦苦设置的行级安全（RLS）**。也就是说，数据库那层"只能看自己数据"的保险，对后端是不生效的。
- **为什么危险**：现在安全的唯一原因，是你在每个接口里都手动写了 `user_id = 当前用户`。只要哪天加新功能时漏写了一行这个过滤，就会变成"A 用户能看到所有人的数据"。这是一种"靠人不犯错"的脆弱设计。
- **怎么判断**：我逐个查了现有 4 个接口 + 日报接口，**目前每一个都正确带了过滤，没有漏的**。所以现在是安全的，风险在于"未来扩展时容易踩坑"。
- **怎么修**：① 短期——在团队规范里写死"任何 `supabase.table(...)` 查询必须带 `user_id` 过滤"，并在 `AGENTS.md` 里加一条给 Codex 的硬规则；② 中长期——考虑前端直连 Supabase + 完全依赖 RLS（这样钥匙偷不走、过滤忘不了），后端只做 AI 和定时任务。

---

### 🟢 低风险（知道即可，有空再优化）

**L1. 邮件内容直接拼 HTML，没有转义**
任务标题、日报内容是直接塞进邮件 HTML 里的。如果任务标题里写了 HTML 标签，邮件排版可能会乱。因为邮件是发给你自己的，且主流邮箱不会执行邮件里的脚本，所以危害很小。有空时把用户内容做一下 HTML 转义即可。

**L2. `SECRET_KEY` 定义了但没用上**
配置里有个 `SECRET_KEY`，但代码里实际没用到它（鉴权走的是 Supabase token）。不是漏洞，属于"无用配置"，留着会让人误以为它在保护什么。建议要么删掉，要么明确它的用途。

**L3. 输入长度没设上限**
任务输入只校验了"不能为空"，没设最大长度。理论上有人能发一段超长文本去消耗 DeepSeek 的输入费用。配合 M1 的限流一起处理就行。

**L4. 定时任务跟着后端进程跑，多实例会重复发**
提醒和日报是用 APScheduler 跟在后端进程里跑的。如果以后后端扩容成多台机器，每台都会跑一遍定时任务，可能导致**同一条提醒被发好几遍**。现在单机没问题，将来扩容前要注意。

**L5. 真实密钥目前存在本地 `backend/.env`**
我确认了本地 `.env` 里是真实密钥（Supabase 服务密钥、DeepSeek、Resend 都有）。这本身没问题（没提交到 git）。提醒两点：① 千万别把这个文件发给别人或截图；② 如果哪天怀疑泄露了，去对应平台一键重置（轮换）密钥即可。

---

### 安全部分一句话总结

**没有致命洞，可以先小范围用。正式放量前，重点处理 M1（限流 + DeepSeek 消费上限）和确认 Supabase 注册需要邮箱验证，就能挡掉绝大多数现实风险。**

---

## 第二部分：微信小程序可行性分析

### 一句话结论

**可行，但不是"把现在的网页套个壳"就能上，需要做 4 处实打实的改造。对你这种个人创业者，我更推荐先走"过渡方案"快速验证，再决定要不要做原生小程序。**

下面先讲清楚为什么不能直接套壳——这 5 个是微信定的硬门槛，绕不过去：

### 五个硬门槛（微信的规矩，不是代码问题）

**门槛 1：后端域名必须 ICP 备案（这是最大的拦路虎）**
微信小程序有个规定：小程序里所有要访问的服务器域名，必须是 HTTPS，而且必须在工信部做过 **ICP 备案**（也就是用国内服务器、域名实名登记）。
你现在的后端在 Railway（海外）、数据库在 Supabase（海外），**这两个都没法备案**，填进微信的"服务器合法域名"里会直接被拒。
👉 这意味着：后端要么搬到国内云（阿里云/腾讯云）+ 备案，要么改用微信云开发（见下方路径 A）。

**门槛 2：登录方式要换**
你现在用的是"邮箱+密码"登录。小程序里这套体验很差，而且审核容易卡。小程序的标准做法是**微信一键授权登录**（用户点一下"用微信登录"，后端拿到微信给的身份码换成用户 ID）。这块需要新写一套登录逻辑。

**门槛 3：前端要重写（这是工作量大头）**
微信小程序不认 React 网页。你现有的 React 前端没法直接搬，需要用 **Taro 或 uni-app** 这类框架重写界面（它们的好处是"写一套，能同时编译成微信小程序、抖音小程序、H5"）。逻辑能复用，但界面代码要重做。

**门槛 4：小程序账号要企业/个体资质**
注册微信小程序需要主体认证。涉及"用户数据、给他人使用"的工具类小程序，基本要**个体工商户或企业营业执照**，加 ¥30/年 的微信认证。个人主体限制很多。你做电商应该已经有营业执照，这一步问题不大。

**门槛 5：提醒方式要从邮件换成"订阅消息"**
小程序里发邮件提醒体验很差。微信原生的做法是**订阅消息**（就是你常收到的那种小程序推送）。这其实是个升级——提醒会直接弹到用户微信里，比邮件强得多。需要改造提醒逻辑。

---

### 三条可行路径对比（按推荐顺序）

| 路径 | 怎么做 | 优点 | 缺点 | 适合谁 |
|---|---|---|---|---|
| **C. 先做手机 H5 过渡（最推荐先做）** | 现有 React 网页几乎不改，挂到一个备案过的国内域名上，做成手机网页，可以分享链接、加到桌面 | 改动最小、最快上线、零学习成本，能马上验证产品有没有人用 | 不是"正经小程序"，没有微信入口和订阅消息推送 | 想先低成本验证、尽快用起来的你 |
| **A. 微信云开发（最推荐做正式版）** | 用微信官方的"云开发"：云函数 + 云数据库，**调用走微信内部通道，免去自己备案后端域名的麻烦** | 个人开发者最省心，免备案后端、按量付费、和小程序天然打通 | 后端逻辑要迁到云函数；调 DeepSeek 需在云函数里出网调用；数据库建议用云开发自带的 | 决定认真做微信原生小程序、又不想折腾服务器备案的你 |
| **B. 自建国内服务器 + 备案** | 阿里云/腾讯云买台轻量服务器 + 域名 + ICP 备案（约 7–20 天）+ HTTPS 证书，把现有 FastAPI 直接部署上去 | 现有后端代码几乎能直接复用、最灵活 | 备案要等、要自己运维服务器、对非技术的你成本最高 | 已经有备案域名、或愿意长期自建的团队 |

**我的建议**：
1. **这两周**：走 **路径 C**，把现有网页挂到备案域名做成手机 H5，先让自己和几个朋友真用起来，验证"AI 解析待办"这个功能你到底用不用得上。
2. **验证有价值后**：再走 **路径 A（微信云开发）** 做正式小程序，享受微信入口 + 订阅消息推送。
3. **路径 B** 除非你已经有备案域名，否则对你性价比最低，可以先不考虑。

> 关于你做抖音电商：如果你的核心用户来自抖音，**抖音小程序**可能比微信更贴近你的生意（能挂在店铺、直播间）。但抖音小程序同样需要域名备案 + 企业资质，门槛和微信差不多。建议先把微信版（或 H5 版）跑通，用 Taro/uni-app 写的前端将来能一键多编译一份抖音版，不浪费。

---

## 第三部分：Codex 执行方案

下面分两块：**(A) 安全修复**和 **(B) 小程序改造**。每个任务都写成可以直接复制给 Codex 的形式。建议先做 A，再做 B。

### 块 A：安全修复（建议立即做，工作量小）

```
【任务 A1：给 AI 解析接口加限流】
目标：防止 /tasks/parse 被刷爆 DeepSeek 账单。
- 在 backend/requirements.txt 增加 slowapi==0.1.9
- 在 backend/main.py 初始化 slowapi 的 Limiter（按登录用户 id 作为限流 key）
- 在 backend/routers/tasks.py 的 parse_only 接口上加装饰器：每个用户每分钟最多 10 次
- 超限时返回 429 状态码和友好中文提示
验收：同一用户 1 分钟内第 11 次调用 /tasks/parse 返回 429。

【任务 A2：给输入加长度上限】
目标：防止超长输入消耗 token。
- 在 backend/models/schemas.py 的 TaskCreate.raw_input 和 TaskConfirm.content 上
  把 Field(min_length=1) 改成 Field(min_length=1, max_length=500)
验收：发送超过 500 字的 raw_input 返回 422 校验错误。

【任务 A3：在 AGENTS.md 加一条数据隔离硬规则】
目标：防止以后扩展时漏写 user_id 过滤导致数据串号。
- 在 AGENTS.md 的规范部分新增一节"数据访问铁律"：
  "因为后端使用 SUPABASE_SERVICE_KEY 会绕过 RLS，所以任何对 tasks /
   daily_reports / users 表的 select/update/delete 查询，都必须显式带上
   .eq('user_id', user.id)（users 表用 .eq('id', user.id)）。新增任何
   数据库查询前，先检查这一条。"
验收：AGENTS.md 中存在该规则。

【任务 A4：邮件内容做 HTML 转义】
目标：消除邮件 HTML 注入/排版错乱。
- 在 backend/services/email_service.py 中，对 task_content、remind_time、
  report_content、report_date 在拼进 html 前用 html.escape() 处理
验收：任务标题含 "<b>test</b>" 时，邮件里显示为纯文本而非加粗。

【任务 A5（手动，非代码）：平台侧加固】
- 登录 Supabase 后台 → Authentication → 开启 "Confirm email"（注册需邮箱验证）
- 登录 DeepSeek 后台 → 设置每月消费上限（兜底防刷）
- 确认 backend/.env 永远不提交、不外发；如怀疑泄露则在各平台轮换密钥
```

### 块 B：微信小程序改造（建议按路径 A「微信云开发」执行）

> 说明：以下是把项目改造成微信小程序的总体施工单。每一步交给 Codex 时，
> 建议一次只给一个任务，让它做完你验证后再给下一个。前端框架选 Taro（React 语法，
> 和你现有 React 代码最接近，迁移成本最低）。

```
【任务 B1：搭建 Taro 小程序骨架】
- 在项目根目录用 Taro 初始化一个微信小程序工程：目录名 miniapp
- 技术栈选 React + TypeScript（可选 JS）
- 配置 project.config.json，appid 先留占位
验收：miniapp 能在微信开发者工具里跑起来，显示空白首页。

【任务 B2：迁移界面与逻辑】
- 把 frontend/src 下的页面/组件逻辑（Home、Tasks、Login、TaskInput、
  TaskList、TaskCard、ParsePreview、DailyReport、TimePickerModal）
  逐个改写成 Taro 组件，样式用 Taro 的 NutUI 或自写
- 数据请求层 frontend/src/api.js 改写成用 Taro.request（替换 axios）
验收：四个主要页面在开发者工具里能正常显示和交互（先连本地 mock）。

【任务 B3：替换登录为微信授权登录】
- 删除邮箱密码登录 UI，改为"微信一键登录"按钮，调用 Taro.login() 拿 code
- 新增后端/云函数接口 /auth/wechat：接收 code，调用微信 jscode2session
  换取 openid，在 users 表里按 openid 建立或查找用户，签发自有会话 token
- 后端 backend/auth.py 的 get_current_user 增加对该自有 token 的校验分支
  （或在云开发中用云函数上下文自带的 openid）
验收：点击"微信登录"后能进入首页，且任务数据与该微信用户绑定。

【任务 B4：后端迁移到微信云开发（免备案路线）】
- 在微信云开发控制台开通环境
- 把 FastAPI 的核心逻辑（解析、增删改查、日报）改写/封装为云函数
- DeepSeek 调用放在云函数内出网请求（在云开发控制台开通云函数外网访问）
- 数据迁移：将 Supabase 的表结构（见 supabase/schema.sql）在云开发数据库中重建
  （tasks / daily_reports / users 三个集合），并设置只能读写本人 openid 数据的权限
验收：小程序通过云函数能完成"输入一句话→AI 解析→保存→列表展示→日报"全流程。

【任务 B5：提醒改为订阅消息】
- 申请一个"任务提醒"订阅消息模板
- 前端在用户确认任务时，引导用户授权订阅消息
- 把 backend/services/reminder.py 的"发邮件"逻辑，改成调用微信
  subscribeMessage.send 推送（定时任务用云开发的"定时触发器"替代 APScheduler）
- 邮件提醒可保留作为备用渠道
验收：到提醒时间，用户微信能收到订阅消息推送。

【任务 B6：合规与提审】
- 补全小程序类目（建议"工具/效率"）、隐私协议、用户信息收集说明
- 用企业/个体营业执照完成主体认证
- 提交审核
验收：小程序通过微信审核并可发布体验版。
```

---

## 附：给你的行动建议（非技术版）

如果让我替你排优先级，就三步：

1. **今天就做**：去 DeepSeek 后台设个每月消费上限，去 Supabase 后台开启注册邮箱验证。这两步点几下鼠标，就堵住了最现实的"被刷钱"风险，不需要写代码。
2. **这周让 Codex 做**：块 A 的 A1–A4 安全修复，工作量很小，做完产品更稳。
3. **想清楚再投入**：小程序改造（块 B）是个不小的工程（前端基本重写 + 登录重做 + 后端迁移）。建议先用"路径 C 手机 H5"花最小成本让产品真正跑起来、确认有用，再决定要不要正式做微信小程序。别一上来就扎进小程序的备案和审核里，那会消耗你大量本该用在生意上的精力。

如果你决定走某一条路径，我可以再帮你把对应的 Codex 任务拆得更细、更具体到每个文件。
