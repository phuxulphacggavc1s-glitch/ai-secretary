# Codex 执行完毕后 · 下一步操作清单

> 目标：从"本地有代码"→"真实可用的线上产品"
> 预计耗时：1-2小时（不含等待审核）

---

## 当前状态确认

✅ 代码已生成（backend + frontend）
✅ 本地服务已启动（前端 5173，后端 8000）
❌ `.env` 文件是占位符，还没有真实密钥
❌ Supabase 数据库表还没建
❌ 还没部署上线

---

## 第一步：注册3个账号，拿到密钥（约30分钟）

按顺序来，每个都有免费额度够用。

---

### 1. Supabase（数据库 + 登录系统）

1. 打开 https://supabase.com → 点 **Start your project** → 用 GitHub 登录
2. 点 **New project**，填项目名（如 `ai-secretary`），设一个数据库密码（记下来）
3. 等待项目初始化（约1分钟）
4. 进入项目后，点左侧菜单 **Project Settings → API**
5. 记下这两个值：
   - `Project URL` → 填到 `SUPABASE_URL`
   - `service_role` 那行的 key → 填到 `SUPABASE_SERVICE_KEY`
   - `anon public` 那行的 key → 填到前端的 `VITE_SUPABASE_ANON_KEY`

---

### 2. DeepSeek（AI解析能力）

1. 打开 https://platform.deepseek.com → 注册账号（手机号即可）
2. 登录后点右上角头像 → **API Keys** → **Create new API key**
3. 复制生成的 key → 填到 `DEEPSEEK_API_KEY`

> 💡 充值建议：先充 10 元，够用很久（解析1条任务大约花 0.001 元）

---

### 3. Resend（发邮件提醒）

1. 打开 https://resend.com → 用 GitHub 或邮箱注册
2. 登录后点左侧 **API Keys** → **Create API Key**
3. 复制 key → 填到 `RESEND_API_KEY`
4. 点左侧 **Domains**，你有两个选择：
   - **快速方案（推荐MVP）**：不配置自定义域名，用 `onboarding@resend.dev` 发邮件，够测试用
   - **正式方案**：绑定自己的域名，按提示加 DNS 记录（需要1-2小时生效）
5. 把发件邮箱填到 `FROM_EMAIL`（测试阶段填 `onboarding@resend.dev`）

---

## 第二步：填写密钥文件（约5分钟）

打开项目文件夹，找到并编辑这两个文件：

### `backend/.env`

```
SUPABASE_URL=https://tvymepukxmwitwfieuhc.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InR2eW1lcHVreG13aXR3ZmlldWhjIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MDM5MDc0NiwiZXhwIjoyMDk1OTY2NzQ2fQ.0d5UR1o8Frn_YWEypRn9W3uGJVmBN0XT_wvBTvNkmv8
DEEPSEEK_API_KEY=sk-e8a55c0f00434b2b8117c00573affc5f
DEEPSEEK_BASE_URL=https://api.deepseek.com
RESEND_API_KEY=re_WzEAfoXV_9kCEvJGV2b5N5E8P1vnhC9M7
FROM_EMAIL=onboarding@resend.dev
SECRET_KEY=随便填32个字母数字（如 abc123def456abc123def456abc12345）
FRONTEND_URL=http://localhost:5173
```

### `frontend/.env.local`

```
VITE_SUPABASE_URL=https://tvymepukxmwitwfieuhc.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InR2eW1lcHVreG13aXR3ZmlldWhjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODAzOTA3NDYsImV4cCI6MjA5NTk2Njc0Nn0.K7sTdgZfFdZNP3s5nFW6NUM05TM_v7m7neiuRq3uCtI
VITE_API_BASE_URL=http://localhost:8000
```

---

## 第三步：在 Supabase 建数据库表（约5分钟）

1. 回到 Supabase 控制台，点左侧菜单 **SQL Editor**
2. 点 **New query**
3. 打开项目里的 `supabase/schema.sql` 文件，把全部内容复制进去
4. 点右上角 **Run**（或按 Ctrl+Enter）
5. 看到 `Success` 就完成了

验证：点左侧 **Table Editor**，应该能看到 `tasks`、`users`、`daily_reports` 三张表。

---

## 第四步：本地重启服务，验证连通（约10分钟）

重启后端（让它加载新的 .env）：

```bash
cd backend
# 如果后端还在跑，先 Ctrl+C 停掉
uvicorn main:app --reload --port 8000
```

重启前端：

```bash
cd frontend
# 如果前端还在跑，先 Ctrl+C 停掉
npm run dev
```

**验证步骤：**

1. 打开 http://127.0.0.1:5173
2. 点"注册"，用你的真实邮箱注册一个账号
3. 登录进去
4. 在输入框里输入："明天下午提醒我给客户报价"
5. 点发送，等待AI解析（3秒内应该出来结果）
6. 确认保存
7. 任务出现在列表里 ✅
8. 检查邮件：Supabase 会发一封验证邮件，点击激活账号

如果上面都通了，说明本地端到端跑通了，可以准备上线。

---

## 第五步：部署上线（约30分钟）

### 后端部署到 Railway

1. 打开 https://railway.app → 用 GitHub 登录
2. 点 **New Project → Deploy from GitHub repo**
3. 选择你的项目仓库（需要先把代码 push 到 GitHub）
4. Railway 会自动识别 Python 项目
5. 点 **Variables**，把 `backend/.env` 里的所有变量逐条填进去（注意 `FRONTEND_URL` 改成你的真实前端域名）
6. 部署完成后，Railway 会给你一个域名如 `https://ai-secretary-backend.railway.app`
7. 记下这个域名

**先把代码推到 GitHub（如果还没做）：**

```bash
cd ai-secretary
git init
git add .
git commit -m "Initial commit"
# 在 GitHub 新建仓库后：
git remote add origin https://github.com/你的用户名/ai-secretary.git
git push -u origin main
```

---

### 前端部署到 Vercel

1. 打开 https://vercel.com → 用 GitHub 登录
2. 点 **Add New Project**，选你的仓库
3. 把 **Root Directory** 设为 `frontend`
4. 点 **Environment Variables**，填入：
   ```
   VITE_SUPABASE_URL = 你的Supabase URL
   VITE_SUPABASE_ANON_KEY = 你的anon key
   VITE_API_BASE_URL = https://你的Railway后端域名
   ```
5. 点 **Deploy**
6. 部署完成后，Vercel 给你一个域名如 `https://ai-secretary.vercel.app`

---

### 最后一步：更新跨域配置

后端 `backend/.env` 里把 `FRONTEND_URL` 改成 Vercel 的真实域名：

```
FRONTEND_URL=https://ai-secretary.vercel.app
```

然后在 Railway 控制台同步更新这个变量，后端会自动重启。

---

## 全部完成后的检查清单

- [ ] 用真实邮箱注册、登录成功
- [ ] 输入一句话，AI 解析出结果
- [ ] 任务保存到数据库，刷新页面还在
- [ ] 设一个"1分钟后"的提醒，等邮件到达
- [ ] 手机浏览器打开 Vercel 域名，页面正常

---

## 遇到问题速查

| 问题现象 | 原因 | 解决 |
|---------|------|------|
| 后端启动报错 `SUPABASE_URL not found` | .env 没填或路径不对 | 确认 .env 在 backend/ 目录下 |
| AI解析返回空/报错 | DeepSeek key 不对 | 检查 key 是否完整复制 |
| 登录后提示 "Invalid token" | Supabase anon key 填错 | 检查前端 .env.local |
| 邮件没收到 | Resend 未验证发件域名 | 先用 onboarding@resend.dev |
| 部署后前端空白页 | 环境变量没填 | 检查 Vercel 的 Environment Variables |
| CORS 错误 | FRONTEND_URL 没更新 | 把 Railway 的 FRONTEND_URL 改成 Vercel 域名 |

---

## 上线后第一周要做的事

1. **邀请10个真实用户**（朋友、团队成员）试用，收集反馈
2. **观察AI解析准确率**：如果经常解析错，收集错误案例，改进 Prompt
3. **不要急着加功能**：先把核心流程用顺了，再迭代
