# AI 秘书 — 正式版 + 公开测试 准备清单

> 目标：买好域名、把邮件跑通，让身边的人（朋友圈/群）都能注册使用，测试有没有商业价值。
> 前提：后端已部署在 Railway，前端在 Vercel，应用本身已经能用。
> 重点提醒：公开测试最大的风险**不是数据安全，是成本失控和新用户体验**。请按顺序来。

---

## ⭐ 先做这 3 件"保命"的事（公开发出去之前必须完成）

### 1. 给 DeepSeek 设每月消费上限（今天就做，5 分钟）
公开后陌生人会来用，每次 AI 解析都花你的 DeepSeek 钱。限流只能挡"单账号狂刷"，挡不住"很多人一起用"或"注册一堆小号"。
👉 登录 DeepSeek 后台 → 费用/账户设置 → 设一个**每月消费上限**（建议先设 ¥50 封顶）。这是你睡得着觉的根本保障。

### 2. 确认 Supabase"注册需邮箱验证"已打开
防止机器人用假邮箱批量注册、刷爆你的额度。
👉 Supabase 后台 → Authentication → 打开 "Confirm email"。
（注意：打开后会引出下面"第二件事"里的发信坑，两件事要配套做。）

### 3. 把没提交的代码推上去（让线上是最新版）
我查到你本地还有改动没提交（reminder.py、api.js、Home.jsx 等）。线上跑的是旧版。
👉 见文末"给 Codex 的小任务"，让它提交并推送，Railway/Vercel 会自动重新部署。

---

## 第一件事：买域名 + 配邮件（让别人能收到提醒）

### 1.1 买域名
- **不需要 ICP 备案！** 你只是拿域名来发邮件，不是在国内服务器上建网站，所以免备案、当天就能用。别被之前小程序的备案要求吓到，那是两回事。
- **去哪买**：阿里云、腾讯云（中文界面、支付宝付款，最省事）；或 Cloudflare、Namecheap（国际，价格透明）。
- **买哪种**：建议 `.com`，几十块一年，最通用、邮件送达率最好。`.top`/`.xyz` 更便宜但有些邮箱会当垃圾邮件，不推荐用来发信。
- 例：买一个 `myassistant.com` 之类的，专门给这个产品用。

### 1.2 在 Resend 验证这个域名
1. 登录 https://resend.com → 左侧 **Domains** → **Add Domain**，填你刚买的域名。
2. Resend 会给你几条 **DNS 记录**（通常是 1 条 TXT/SPF + 几条 DKIM，可能还有 1 条 MX）。
3. 回到你买域名的后台（阿里云/腾讯云/Cloudflare）→ 找到"DNS 解析/域名解析"→ 把 Resend 给的记录**一条条照抄添加**进去（类型、主机记录、记录值对应填）。
4. 回 Resend 点 **Verify**，等几分钟到几小时（DNS 生效需要时间）。看到域名状态变成 **Verified（绿色）** 就成了。

### 1.3 改后端的发件地址
👉 Railway → 你的项目 → Variables → 把 `FROM_EMAIL` 改成你的域名邮箱，例如：
```
FROM_EMAIL=noreply@你的域名.com
```
改完 Railway 自动重启。从此提醒邮件、日报能发给**任何人**，不再只限你自己。

---

## 第二件事：解决"新用户收不到注册确认邮件"的坑

**这是最容易被忽略、又最致命的一个点。**

Supabase 免费版自带的发信功能限制极严（每小时只能发几封，官方明说不能用于生产）。你一公开，几十人同时注册，确认邮件根本发不出来，新用户卡在第一步就流失了——你会误以为"产品没人用"，其实是注册都没进去。

**解决办法：把 Resend 接到 Supabase 当发信通道（自定义 SMTP）。** 你域名都验证好了，顺手就能配：

1. 在 Resend 后台 → **API Keys** 里新建一个 Key（或用现有的），并找到 Resend 的 **SMTP** 连接信息（Host、Port、用户名、密码）。Resend 文档里有 SMTP 一节。
2. Supabase 后台 → **Project Settings → Authentication → SMTP Settings** → 打开 **Enable Custom SMTP**，把 Resend 的 SMTP 信息填进去，发件人写 `noreply@你的域名.com`。
3. 保存后，发一封测试注册，确认能收到确认邮件。

配好后，注册确认邮件走的就是你自己的 Resend，额度大、不卡，公开测才扛得住。

---

## 第三件事：守住钱包 + 看清成本

公开测大概会产生这些花费，心里有个数：

| 项目 | 免费额度 | 公开测可能的情况 | 建议 |
|---|---|---|---|
| **DeepSeek** | 充多少用多少 | 用户越多花越多，但单价很低 | **必设月上限 ¥50**，看实际消耗再调 |
| **Resend 邮件** | 免费 100 封/天、3000 封/月 | 几十人 × 每日提醒+日报，可能逼近上限 | 先用着，接近上限再考虑升级 |
| **Railway 后端** | 约 $5 免费额度/月 | 后端 24 小时常开 + 定时任务，免费额度可能月底就用完 | 用完会停机，需升级 Hobby（约 $5/月）保持常开 |
| **Vercel 前端** | 个人免费额度很足 | 基本够用 | 不用担心 |
| **域名** | — | 一次性 | 几十元/年 |

**一句话**：真金白银的支出很小（域名几十块 + 可能 Railway 几十块/月），但 **DeepSeek 不设上限是唯一可能"爆雷"的点**，务必先设。

---

## 你代码现在的真实状态（我帮你查过了）

- ✅ **限流已生效**：`/tasks/parse` 限制每用户每分钟 10 次（commit b3b607c 已提交）。
- ✅ 限流依赖 `slowapi` 已在 requirements，已接入 main.py 和 tasks.py。
- ⚠️ **有未提交改动**：`backend/services/reminder.py`、`frontend/src/api.js`、`frontend/src/pages/Home.jsx` 等。线上跑的还不是这些最新改动，需提交推送。

---

## 建议的上线顺序（照这个节奏走最稳）

1. **今天**：DeepSeek 设月上限 → Supabase 打开邮箱验证 → 让 Codex 把未提交代码推上去。
2. **买好域名后**：在 Resend 验证域名 → 改 Railway 的 `FROM_EMAIL` → 在 Supabase 配 Resend 自定义 SMTP。
3. **自测一遍**：用一个**不是你自己**的邮箱（比如家人的）走完整流程：注册→收到确认邮件→登录→建任务→收到提醒邮件。全程通了，才说明"别人也能用"。
4. **小范围先发**（强烈建议）：先发一个十几人的群测一两天，观察 DeepSeek 消耗、有没有报错、大家反馈。没问题了再发朋友圈大范围公开。这样万一有坑，损失可控。
5. **公开后**：每天瞄一眼 DeepSeek 消耗和 Railway 状态。

> 我的建议：你要测的是"有没有商业价值"，这件事其实十几个真实用户认真用几天，就能看出大概。不一定非要一上来就大公开。小范围跑通、确认有人愿意持续用，再放大，对你最划算。

---

## 给 Codex 的小任务（提交未推送的改动）

```
背景：本地有未提交改动需要部署到线上。仓库已配置 origin，远程为 GitHub。
【任务】
1. 运行 git status，确认改动文件（reminder.py、api.js、Home.jsx、配置文件等）。
2. 确认 .env / .env.local / DEPLOY_ENV.txt 不在待提交列表中（绝不能提交密钥）。
3. cd backend && python -m pytest -q，确认测试通过（失败先报告，不要硬提交）。
4. cd frontend && npm run build，确认能打包成功。
5. 回根目录，git add 源码与配置改动，git commit -m "feat: 正式版前的功能与配置更新"，
   git push origin main。
6. 推送后确认 git show HEAD --name-only 里没有任何 .env 文件。
验收：推送成功、无密钥泄露，Railway/Vercel 自动触发重新部署。
```
