# V3 上线部署清单 —— 秘书对话流 + 记忆层 + 字段精简

> 日期：2026-07-06。本次改动已通过：后端 34 个测试全部通过、前端 vite build 构建成功。

## V4 追加（和 V3 一起发）

1. **企业微信双向对话**：在企业微信里直接跟秘书聊天。代码已就绪，需按
   《企业微信双向对话_配置手册.md》在企业微信后台配置（约 20 分钟），不配不影响其他功能。
2. **随聊随记忆**：聊天中 AI 自动记住重要事实（上限 15 条自动去重）；聊天卡片右上角
   放大镜可全文搜索历史对话。
3. **秘书人设「小e」**：`backend/services/persona.py` 一个文件统一管理秘书性格，
   改它就能全局调口吻。
4. 新增依赖 `pycryptodome`（部署命令里的 pip install 会自动装）。

## 这次改了什么（一句话版）

1. **删掉了「目标 / 完成标准 / 相关人」三个字段**——记待办的弹窗只剩：内容、分类、优先级、提醒时间。
2. **首页改成对话流**——打开页面秘书会主动开口（今天该干嘛、哪件事要催、要你拍板的直接问你），你可以直接回它。同一个输入框既能记待办也能聊天，AI 自动识别。
3. **加了记忆层**——AI 每周日晚 9 点自动把你近 60 天的任务历史提炼成"用户画像"（哪类事爱拖、常跟谁打交道、完成率），之后的开场白和建议都带着这份记忆，越用越准。首次使用会自动生成一次，不用等周日。

## 界面位置（在哪能看到）

- **对话流**：首页最上方，代替了原来的输入框和大紫色横幅。开场白下面有可点的快捷回复气泡。
- **今日概况**：对话框下方，白底紧凑数据条（今日完成 / 进行中 / 逾期）。
- 其余（最该先干、催办、逾期、进度问询、最近任务）位置不变。

## ECS 实操命令（复制粘贴版）

```bash
# ① 你电脑：推代码
git add -A && git commit -m "V3: 对话流+记忆层+删冗余字段+头像表情" && git push

# ② 阿里云 Workbench：更新后端
cd /var/www/ai-secretary && git pull
cd backend && source venv/bin/activate && pip install -r requirements.txt
systemctl restart ai-secretary
curl http://127.0.0.1:8000/health   # 应返回 {"status":"ok"}

# ③ Supabase SQL Editor：运行 supabase/upgrade_v3_chat_memory.sql（必须在②之后）

# ④ 阿里云 Workbench：重建前端
cd /var/www/ai-secretary/frontend && npm ci && npm run build
# 完成后手机强制刷新 https://mede-in-ai.com
```

## 上线步骤（顺序不能反）

**第 1 步：部署新后端**（Railway 或阿里云 ECS，按你现有流程）
新增文件：`backend/services/memory.py`、`backend/services/secretary_chat.py`
改动文件：`main.py`、`models/schemas.py`、`routers/tasks.py`、`routers/secretary.py`、`services/ai_parser.py`、`services/followup.py`、`services/wecom.py`

> 为什么先部署后端：老后端保存任务时还会写那三个字段，如果先删数据库列，老后端会报错。

**第 2 步：执行数据库脚本**
Supabase 后台 → SQL Editor → 粘贴运行 `supabase/upgrade_v3_chat_memory.sql`
（删三列 + 建 `secretary_messages` 对话表 + 建 `user_memory` 画像表，都带了 RLS）

**第 3 步：部署前端**（Vercel，按你现有流程）
新增：`src/components/SecretaryChat.jsx`
改动：`src/pages/Home.jsx`、`src/api.js`、`ParsePreview.jsx`、`TaskCard.jsx`、`SecretaryBriefing.jsx`

## 上线后怎么验证（3 分钟）

1. 打开首页 → 秘书应该主动说话（第一次会稍慢几秒，它在生成你的画像）。
2. 输入「明天下午提醒我给供应商打款」→ 应弹出确认框，字段只有 4 个。
3. 输入「今天先做什么好」→ 应得到结合你任务的对话回复，而不是弹确认框。
4. 刷新页面 → 对话记录还在；3 小时内秘书不会重复开场。

## 两个要知道的点

- **费用**：对话、开场白、画像都走 DeepSeek，调用量会比以前多。已加限流（聊天 10 次/分钟、开场白 6 次/分钟），但建议确认 DeepSeek 后台的每月消费上限还开着。
- **主动提醒的边界**：网页关掉后无法弹消息，"主动找你"仍靠企业微信推送 + 打开页面时的开场白。真正的随时推送要等小程序版（订阅消息），那是下一个项目。

## 新接口（备查）

| 接口 | 作用 | 限流 |
|---|---|---|
| GET /secretary/messages | 拉对话记录 | - |
| GET /secretary/opening | 秘书主动开场白（3小时内不重复） | 6次/分钟 |
| POST /secretary/chat | 发消息（自动识别记待办/聊天） | 10次/分钟 |
